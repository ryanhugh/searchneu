/*
 * This file is part of Search NEU and licensed under AGPL3.
 * See the license file in the root folder for details.
 */

import macros from '../recordGenerator';

it('generates a proper class record from a class JSON', async () => {
  const profInfo = {
    name: 'Henry Armitage',
    firstName: 'Henry',
    lastName: 'Armitage',
    id: 'spookyscaryskeletons',
    phone: '1234567890',
    emails: ['h.armitage@mistkatonic.edu'],
    primaryRole: 'Chief Librarian',
    primaryDepartment: 'Not Computer Science',
    link: 'https://google.com',
  };

  
});

it('generates a proper section record from a section JSON', () => {
});

it('generates a proper professor record from a professor JSON', () => {
});
